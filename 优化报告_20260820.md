# 凌晨优化报告 2026-08-20

- 数据源: 3/3 ok (push2 0.21s / qt.gtimg 0.17s / 新浪 K线 0.18s), cron_missing=0。
- 🆕 **P1 修复** (v4.22): `daily_diagnostic.py` cron 检查新增 `if not j.get("enabled", True): continue`,过滤掉 10 个 `enabled=False` 的 cron,避免日报把"主动禁用任务"误标"✅存在"。修复前 cron_jobs_count=6, 修复后=1 (仅剩 en=True 且有 script 的)。引用 `scripts/daily_diagnostic.py.bak-20260820-0205` + git diff 4 行。
- 回测: 8/19 delta status=OK, drift=0, fail=0, 5 只样本 wr_avg=30% 仍偏低, 暂不调参 (基线不足)。
- 死产物: `runtime/reports/decision_dashboard_latest.md` 静态停更 7/31, 无 cron 引用, 建议下个凌晨归档。
- 持仓: 300059 浮亏 -8.67% 已破默认 -8% 止损线, 按 USER.md"破止损持仓不动观察"姿态, 不需新增告警。
- 清理: 7 天前 .log 候选=0, bak>30 天=0; __pycache__ 共 212K (无收益, 保留给脚本下次启动加速)。
